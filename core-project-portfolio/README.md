# Project Portfolio — RevoGrid Core

A realistic project portfolio showing two-level row grouping, sorting, filtering, editing, and custom cells with only the MIT-licensed RevoGrid Core package.

Run `pnpm dev:vue`, `pnpm dev:ts`, `pnpm dev:react`, or `pnpm dev:angular`.
