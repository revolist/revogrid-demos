import { ChangeDetectionStrategy, Component, Input, ViewEncapsulation, computed, signal } from '@angular/core'; import { RevoGrid } from '@revolist/angular-datagrid'; import { createGrouping, PROJECT_COLUMNS, PROJECTS } from './project-portfolio.shared'; import '@fortawesome/fontawesome-free/css/all.min.css'; import './project-portfolio.css';
@Component({ selector:'project-portfolio-demo', standalone:true, imports:[RevoGrid], changeDetection:ChangeDetectionStrategy.OnPush, encapsulation:ViewEncapsulation.None, template:`<section class="portfolio-demo" [class.is-dark]="isDark"><div class="portfolio-toolbar"><div class="portfolio-grouping"><span>Grouped by</span><strong>Department → Status</strong></div><button class="portfolio-toggle" type="button" [attr.aria-label]="expanded() ? 'Collapse all groups' : 'Expand all groups'" [attr.title]="expanded() ? 'Collapse all groups' : 'Expand all groups'" (click)="expanded.set(!expanded())"><i class="fa-solid" [class.fa-angles-up]="expanded()" [class.fa-angles-down]="!expanded()" aria-hidden="true"></i></button></div><revo-grid class="portfolio-grid" [theme]="isDark ? 'darkCompact' : 'compact'" [source]="projects" [columns]="columns" [grouping]="grouping()" [filter]="true" [range]="true" [resize]="true" [hideAttribution]="true" [canMoveColumns]="true" [rowSize]="44"></revo-grid></section>` })
export class ProjectPortfolioDemoComponent { @Input() isDark=false; readonly projects=PROJECTS; readonly columns=PROJECT_COLUMNS; readonly expanded=signal(true); readonly grouping=computed(()=>createGrouping(this.expanded())); }
