import { useMemo, useState } from 'react'; import { RevoGrid } from '@revolist/react-datagrid'; import { createGrouping, PROJECT_COLUMNS, PROJECTS } from './project-portfolio.shared'; import '@fortawesome/fontawesome-free/css/all.min.css'; import './project-portfolio.css';
export default function ProjectPortfolioDemo({ isDark = false }: { isDark?: boolean }) { const [expanded, setExpanded] = useState(true); const columns = useMemo(() => PROJECT_COLUMNS, []); const grouping = useMemo(() => createGrouping(expanded), [expanded]); const toggleLabel = expanded ? 'Collapse all groups' : 'Expand all groups'; return <section className={`portfolio-demo${isDark ? ' is-dark' : ''}`}><div className="portfolio-toolbar"><div className="portfolio-grouping"><span>Grouped by</span><strong>Department → Status</strong></div><button className="portfolio-toggle" type="button" aria-label={toggleLabel} title={toggleLabel} onClick={() => setExpanded(value => !value)}><i className={`fa-solid ${expanded ? 'fa-angles-up' : 'fa-angles-down'}`} aria-hidden="true" /></button></div><RevoGrid className="portfolio-grid" theme={isDark ? 'darkCompact' : 'compact'} source={PROJECTS} columns={columns} grouping={grouping} filter={true} range={true} resize={true} hideAttribution={true} canMoveColumns={true} rowSize={44} /></section>; }
